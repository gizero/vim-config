" Vim settings, rather than Vi compatible (too crazy!)
set nocompatible

" enable syntax highlighting
syntax enable

" enable loading filetype plugins
filetype plugin on

" finding files in all subdirs beneath cwd
set path+=**

" display all matching files when we tab complete
set wildmenu
